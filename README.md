# wepapp

go straight to localhost:3000/#four is where my problem is

set DEBUG=myapp:* & npm start to run app

also run the sql script and add import the csv file in the clubs folder to your heidisql/equivilant
